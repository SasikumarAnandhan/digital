pipelines:
  default:
    - step:
        name: Sync GitHub Mirror
        image: alpine/git:latest
        clone:
          enabled: false
        script:
          - git clone --bare https://SasikumarAnandhan@bitbucket.org/sasi-kumar/digital.git
          - cd RepoName.git
          - git push --mirror https://github.com/SasikumarAnandhan/digital.git